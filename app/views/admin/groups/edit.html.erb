<%= link_to t(:Administration_Section), admin_root_path %> -> <%= link_to(t(:Groups), admin_groups_path) %> -> <%= t(:Editing_a_group) %> 
<%= error_messages_for :group %>
<% form_for [:admin, @group] do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
    <tfoot>
      <tr>
        <td colspan='6'>
          <%= submit_tag t(:Update) %>
        </td>
      </tr>
    </tfoot>
  </table>
<% end %>