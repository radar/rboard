<%= link_to t(:Administration_Section), admin_root_path %> -> <%= link_to t(:Ranks), admin_ranks_path %> -> <%= t(:Creating_a_rank) %>
<% form_for [:admin, @rank] do |f| %>
  <table cellspacing='0' cellpadding='5' class='action-box' width='100%'>
    <thead>
      <tr>
        <td colspan='2'><h2><%= t(:Creating_a_rank) %></h2></td>
      </tr>
    </thead>
    <%= render :partial => "form", :locals => { :f => f } %>
    <tfoot>
       <tr><td colspan='2'><%= submit_tag "Create" %></td></tr>
    </tfoot>
  </table>
<% end %>